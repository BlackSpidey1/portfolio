import styles from "./Main.module.scss";

import Router from "@/router";

const Main = () => {
  return (
    
    <main className={styles.main}>
      <Router />
    </main>
  );
};

export default Main;
